name: check24-backend
description: A check24 hackatum challange for the project with heavy precomputations for closest artisans
runtime:
  name: nodejs
  options:
    packagemanager: pnpm
config:
  pulumi:tags:
    value:
      pulumi:template: aws-typescript
